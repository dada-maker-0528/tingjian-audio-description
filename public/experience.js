import {icon,main,fillIcons,icons} from './app.js';
import {defaults,newTask,confirmStage,canComplete,applyFeedback} from './flow.js';
import {SpeechService} from './speech-service.js';
Object.assign(icons,{pause:'<path d="M8 5v14M16 5v14" stroke-width="4"/>',replay:'<path d="M3 10a9 9 0 119 11M3 4v6h6"/>',back10:'<path d="M4 9a8 8 0 111 9M4 3v6h6"/><path d="M9 12v5M12 12h3v5h-3z"/>',forward10:'<path d="M20 9a8 8 0 10-1 9M20 3v6h-6"/><path d="M9 12v5M12 12h3v5h-3z"/>',chat:'<path d="M21 11a8 8 0 01-8 8H7l-5 3V11a9 9 0 0119 0zM7 9h9M7 13h6"/>',mic:'<rect x="9" y="2" width="6" height="13" rx="3"/><path d="M5 10v2a7 7 0 0014 0v-2M12 19v3M8 22h8"/>',search:'<circle cx="10" cy="10" r="6"/><path d="m15 15 6 6"/>',fullscreen:'<path d="M3 8V3h5M16 3h5v5M21 16v5h-5M8 21H3v-5"/>',clock:'<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>',spark:'<path d="m12 3 2.4 6.6L21 12l-6.6 2.4L12 21l-2.4-6.6L3 12l6.6-2.4zM20 2v4M18 4h4"/>',save:'<path d="M5 3h14v18l-7-4-7 4z"/>',file:'<path d="M14 3H5v18h14V8zM14 3v5h5M9 13l5 3-5 3z"/>'});
const $=s=>document.querySelector(s), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const time=n=>`${Math.floor(Math.max(0,n||0)/60).toString().padStart(2,'0')}:${Math.floor(Math.max(0,n||0)%60).toString().padStart(2,'0')}`;
const KEY='tingjian-demo-v1';
let stored={};try{stored=JSON.parse(localStorage.getItem(KEY)||'{}')||{};}catch{}
const validSettings=s=>s&&['normal','slow'].includes(s.speed)&&['balanced','concise'].includes(s.density)&&[0.88,1].includes(s.gain);
let prefs={guide:false,reader:false,shortcuts:true,...stored.prefs};
let library=Array.isArray(stored.library)?stored.library.filter(x=>x&&typeof x.id==='string'&&validSettings(x.settings)):null;
if(!library)library=[{id:'demo-original',title:'雨停之前',description:'一场雨，一间老钟表铺。一段意想不到的相遇，让匆忙的脚步慢了下来。',settings:defaults(),position:0,created:1}];
let task=stored.draft&&validSettings(stored.draft.candidate)?stored.draft:null;
let route='home',watching=null,film=null,guideToken=0,runId=0,toastId,roleSequence=false,roleIndex=0,returnFocus=null,lastMessage='',currentGuide='home',previewUrl=null;
const speech=new SpeechService();let guideAbort=null,guideResolver=null,mediaAbort=null,activeNarrationCues=null,currentAudioSource='local';
const liveSpeech=()=>speech.available&&prefs.ttsMode!=='local';
let player=null,playing=false,playerReady=false,clipStart=0,clipEnd=200,narrationOn=true,captionOn=true,lastSavedPosition=0,raf=0,mediaGeneration=0;
const narration=$('#narration'),guide=$('#guide-audio'),modal=$('#modal'),chat=$('#chat');
const roles=[
 {name:'林夏',detail:'齐下巴的黑色短发，米色风衣，棕色斜挎包。第一次出场时站在钟表铺门前。',image:'scene-1.png',pos:'39% 46%',size:'260%'},
 {name:'陈伯',detail:'灰色短发，细框眼镜，深棕色开衫。钟表铺的师傅，坐在工作台旁修表。',image:'scene-2.png',pos:'51% 45%',size:'290%'},
 {name:'小宇',detail:'黑色短发，藏蓝色雨衣，红色书包。来到店里，取爷爷修好的怀表。',image:'scene-2.png',pos:'86% 43%',size:'310%'}
];
const stageNames={roles:'认识角色',short:'7 秒样片',medium:'45 秒样片',verify:'7 秒复验',full:'完整制作',complete:'制作完成'};
const guideText={home:'这里是你的视频库。按空格创建新视频。按数字键播放当前编号的视频。',upload:'请选择视频文件，或者使用演示视频。',library:'这里是保存的全部视频，可以搜索，也可以继续播放。'};
function persist(){try{localStorage.setItem(KEY,JSON.stringify({prefs,library,draft:task}));return true;}catch{announce('这台设备暂时无法保存数据，请释放浏览器存储后重试。');return false;}}
function announce(text){$('#status').textContent='';queueMicrotask(()=>$('#status').textContent=text);}
function toast(text){clearTimeout(toastId);$('#toast').hidden=false;$('#toast').textContent=text;announce(text);toastId=setTimeout(()=>$('#toast').hidden=true,5000);}
function focusTitle(){requestAnimationFrame(()=>{const h=main.querySelector('h1');if(h){h.tabIndex=-1;h.focus({preventScroll:true});}window.scrollTo({top:0,behavior:'instant'});});}
function stopGuide(){guideToken++;guideAbort?.abort();guideAbort=null;if(guideResolver){guideResolver(false);guideResolver=null;}roleSequence=false;guide.pause();guide.currentTime=0;window.speechSynthesis?.cancel();document.querySelectorAll('.role-card.speaking').forEach(x=>x.classList.remove('speaking'));}
function pauseMedia(){if(player)player.pause();narration.pause();playing=false;cancelAnimationFrame(raf);updatePlayButtons();}
function stopAll(){stopGuide();pauseMedia();}
function updateGuideUI(){
 $('#guide-status').textContent=prefs.reader?'读屏优先':prefs.guide?'已开启':'未开启';
 const notice=document.querySelector('.demo-notice>span:nth-of-type(2)');if(notice)notice.textContent=speech.available?(liveSpeech()?'演示模式：画面分析和语音识别为预设；语音合成已接入豆包':'演示模式：画面分析和语音识别为预设；当前使用本地演示音频'):'演示模式：AI 分析、生成与识别结果为预设演示';
}
async function say(key,text,force=false){
 if((!prefs.guide||prefs.reader)&&!force){if(text)announce(text);return false;}
 pauseMedia();stopGuide();const token=guideToken;guideAbort=new AbortController();
 const words=text||film?.guides[key]||guideText[key];if(!words)return false;
 let src=null;
 if(liveSpeech()){
  try{src=await speech.guide(words,guideAbort.signal);}catch(e){if(token!==guideToken||e.name==='AbortError')return false;toast('在线语音暂时不可用，正在使用本地提示音。');}
 }
 if(token!==guideToken)return false;
 if(!src&&film?.guides[key])src=`assets/guide-${key}.m4a`;
 if(src){guide.src=src;return new Promise(resolve=>{guideResolver=resolve;guide.onended=()=>{guideResolver=null;resolve(token===guideToken);};guide.onerror=()=>{guideResolver=null;toast('语音提示暂时无法播放，请重试。');resolve(false);};guide.play().catch(()=>{guideResolver=null;toast('请点击“重听提示”开始播放。');resolve(false);});});}
 if(!window.speechSynthesis){toast('当前浏览器不支持这段语音，请使用文字提示。');return false;}
 return new Promise(resolve=>{guideResolver=resolve;const utterance=new SpeechSynthesisUtterance(words);utterance.lang='zh-CN';utterance.rate=.95;utterance.onend=()=>{guideResolver=null;resolve(token===guideToken);};utterance.onerror=()=>{guideResolver=null;resolve(false);};window.speechSynthesis.speak(utterance);});
}
function clearPlayer(){mediaAbort?.abort();mediaAbort=null;activeNarrationCues=null;mediaGeneration++;pauseMedia();if(player){player.removeAttribute('src');player.load();player=null;}narration.removeAttribute('src');narration.load();playerReady=false;}
function page(html,guideKey,focus=true){clearPlayer();main.innerHTML=html;const heading=main.querySelector('h1');if(heading)heading.tabIndex=-1;fillIcons();$('.main-nav [data-action="home"]').classList.toggle('active',route==='home');$('.main-nav [data-action="library"]').classList.toggle('active',route==='library');currentGuide=guideKey||route;if(focus)focusTitle();updateGuideUI();}
function goHome(isLibrary=false){runId++;stopAll();route=isLibrary?'library':'home';renderHome();say(currentGuide);}
function homeIntro(){return `<div class="guide-banner"><span class="icon-circle">${icon('headphones')}</span><div><strong>${prefs.guide?'语音引导已开启，随时听取操作提示':'让声音陪你完成每一步'}</strong><p>${prefs.reader?'读屏优先已开启，站内语音保持安静。':'可以使用鼠标，也可以用 Tab 和空格完成操作。'}</p></div><button class="text-btn" data-action="${prefs.guide?'repeat-guide':'enable-guide'}">${prefs.guide?'重听提示':'开启语音引导'} ${icon('arrow')}</button></div>`;}
function filmCard(item,i,featured){const pos=item.position>1&&item.position<198?item.position:0;return `<article class="film-card ${featured?'featured':''}"><div class="film-cover"><img src="assets/scene-${i%3+1}.png" alt="${['雨中的老钟表铺，暖光映在石板路上','钟表铺内，林夏、陈伯和小宇相遇','夕阳下，小宇将黑伞递给林夏'][i%3]}" loading="${i?'lazy':'eager'}"><span class="corner-tag">${icon('headphones')}中文口述影像</span><span class="duration">03:20</span></div><div class="film-info"><div class="film-meta"><span class="index">${i+1}</span>${item.id==='demo-original'?'原创模拟短片':'我的口述版'} · 生活故事</div><h3>${esc(item.title)}</h3><p>一场雨，一间老钟表铺。<br>一段意外的相遇，让匆忙的脚步慢了下来。</p><span class="status-label">${icon('check')}${pos?'已听到 '+time(pos):'已添加口述影像'}</span><button class="btn primary" data-action="watch" data-id="${esc(item.id)}" aria-label="${pos?'继续播放':'播放'}《${esc(item.title)}》，3 分 20 秒">${icon('play')}${pos?'继续播放':'播放视频'}</button></div></article>`;}
function createTile(){return `<button class="create-tile" data-action="create"><span class="plus-circle">${icon('plus')}</span><h3>把想看的，变成能听的</h3><p>上传视频，或从演示短片开始</p><span class="tile-link">创建我的口述影像 ${icon('arrow')}</span></button>`;}
function renderHome(query=''){
 const all=route==='library',items=library.filter(x=>x.title.includes(query));
 page(`<div class="container"><div class="page-heading"><div><div class="eyebrow">YOUR STORIES, WITH SOUND</div><div class="title-line"><h1>${all?'我的全部视频':'我的视频'}</h1><span class="count">${library.length} 部</span></div><p>选一部，听见故事。也可以创造新的体验。</p></div><button class="btn primary" data-action="create">${icon('plus')}创建新视频<kbd>空格</kbd></button></div>${!all?homeIntro():`<label class="search-box">${icon('search')}<input type="search" id="search" placeholder="搜索视频名称" aria-label="搜索我的视频" value="${esc(query)}"></label>`}${task&&!task.completed?`<div class="draft-strip">${icon('file')}<span>上次制作停在「${stageNames[task.stage]||'认识角色'}」</span><button class="text-btn" data-action="resume">继续上次制作 ${icon('arrow')}</button></div>`:''}<div class="section-heading"><h2>${all?'全部作品':'最近添加'}</h2>${!all&&library.length>1?`<button class="text-btn" data-action="library">查看全部 ${icon('arrow')}</button>`:'<span>保存在这台设备上</span>'}</div><div class="library-grid" id="cards">${items.length?items.map((x,i)=>filmCard(x,i,!all&&items.length===1)).join(''):(all?'<div class="empty-state"><h3>没有找到相关视频</h3><p>换个名称再试试。</p></div>':'<div class="empty-state"><h3>还没有保存视频</h3><p>按空格开始制作第一个视频。</p></div>')}${!all?createTile():''}</div>${!all?`<div class="workflow-note"><div><h3>一点点确认，安心听完整片</h3><p>每一步，都有语音陪伴。</p></div><div class="mini-steps"><div class="mini-step"><span class="step-num">01</span><div><strong>先认识故事里的人</strong><small>听角色介绍，熟悉人物</small></div></div>${icon('chevron')}<div class="mini-step"><span class="step-num">02</span><div><strong>先听 7 秒，再听 45 秒</strong><small>告诉 AI 你的感受，随时调整</small></div></div>${icon('chevron')}<div class="mini-step"><span class="step-num">03</span><div><strong>保存，随时再听</strong><small>喜欢的故事，留在你的视频库</small></div></div></div></div>`:''}</div>`,all?'library':'home',false);
 if(all)$('#search').addEventListener('input',e=>{const q=e.target.value;$('#cards').innerHTML=library.filter(x=>x.title.includes(q)).map((x,i)=>filmCard(x,i,false)).join('')||'<div class="empty-state"><h3>没有找到相关视频</h3><p>换个名称再试试。</p></div>';});
}
function stageNav(active){const labels=['上传视频','认识角色','试听短片','试听长一点','完成整片'];return `<nav class="stage-nav" aria-label="制作阶段">${labels.map((name,i)=>`${i?'<span class="stage-line" aria-hidden="true"></span>':''}<button class="stage-button ${active===i?'active':active>i?'completed':''}" data-action="view-stage" data-stage="${i}" ${i>active?'disabled':''} ${i===active?'aria-current="step"':''}><span class="num">${i<active?icon('check'):i+1}</span><span>${name}</span></button>`).join('')}</nav>`;}
function shell(content,active){return `<div class="container"><div class="back-row"><button class="text-btn" data-action="exit">${icon('back')}返回我的视频</button><span class="task-name">${active===0?'新建口述影像':'雨停之前'}<span aria-hidden="true"> · </span>${active===0?'从一段视频开始':'03:20 · 中文模拟短片'}</span></div>${stageNav(active)}${content}</div>`;}
function renderUpload(){route='upload';page(shell(`<div class="stage-header"><div class="eyebrow">01 / 选择素材</div><h1>先选择你想听的视频</h1><p>从一段视频开始，我们会先介绍故事里的人。</p></div><div class="upload-layout"><div class="upload-box" id="drop-zone">${icon('upload')}<h2>上传视频文件</h2><p>拖到这里，或选择设备上的视频</p><div class="upload-actions"><button class="btn primary" data-action="upload">${icon('plus')}选择视频文件</button><button class="btn" data-action="link">${icon('link')}粘贴链接</button></div></div><div class="demo-source"><span class="label">第一次来？从演示短片开始</span><div class="source-top"><img src="assets/scene-1.png" alt="雨中的钟表铺"><div><h3>雨停之前</h3><p>3 分 20 秒 · 中文对白 · 3 位角色</p></div></div><p>已准备好对应的人物介绍和口述旁白，可完整体验试听、调整和保存。</p><button class="btn block" data-action="demo">使用演示视频 ${icon('arrow')}</button></div></div><p class="inline-note">${icon('info')}本地文件可选择和预览；预设 AI 分析仅适用于内置演示短片。</p><div id="local-preview"></div>`,0),'upload');const dz=$('#drop-zone');dz.addEventListener('dragover',e=>{e.preventDefault();dz.style.borderColor='#111';});dz.addEventListener('dragleave',()=>dz.style.borderColor='');dz.addEventListener('drop',e=>{e.preventDefault();if(e.dataTransfer.files[0])handleFile(e.dataTransfer.files[0]);});say('upload');}
function beginCreate(){stopAll();if(task&&!task.completed){showModal('继续上次制作？',`<p class="dialog-copy">你还有一份停在「${stageNames[task.stage]||'认识角色'}」的草稿。</p><div class="dialog-actions"><button class="btn" data-action="new-task">重新开始</button><button class="btn primary" data-action="resume">继续上次制作</button></div>`);return;}task=null;renderUpload();}
function startDemo(){closeModal();task=newTask();persist();processing('analyzing','正在认识这个故事',['读取视频','整理出场人物','划分镜头与场景','整理旁白的插入位置'],()=>{route='roles';renderRoles();if(prefs.guide&&!prefs.reader)playRoles();},0);}
function processing(kind,title,steps,done,active){route=kind;stopAll();const id=++runId;page(shell(`<section class="processing"><div class="process-icon">${icon('spark')}</div><h1>${title}</h1><p>《雨停之前》 · 03:20<br>${kind==='analyzing'?'演示模式 · 正在准备预设人物与镜头':liveSpeech()?'正在准备素材，随后在线合成旁白':'正在准备本地演示音频'}</p><div class="progress-track" role="progressbar" aria-label="准备进度" aria-valuemin="0" aria-valuemax="${steps.length}" aria-valuenow="0"><div class="progress-fill" style="width:0%"></div></div><div class="process-list">${steps.map((x,i)=>`<div class="process-item" data-process="${i}">${icon('clock')}<span>${x}</span></div>`).join('')}</div></section>`,active),kind);say(kind==='analyzing'?'analyzing':kind==='full'?'full':'generating');let i=0;const tick=()=>{if(id!==runId)return;if(i<steps.length){const row=main.querySelector(`[data-process="${i}"]`);row.classList.add('current');row.innerHTML=icon('check')+`<span>${steps[i]}</span>`;$('.progress-fill').style.width=((i+1)/steps.length*100)+'%';$('.progress-track').setAttribute('aria-valuenow',i+1);announce(steps[i]+'，已完成');i++;setTimeout(tick,650);}else{stopGuide();done();}};setTimeout(tick,500);}
function rolesHTML(){return `<div class="role-grid">${roles.map((r,i)=>`<article class="role-card" data-role="${i}"><div class="role-portrait" role="img" aria-label="${r.name}的人物画面" style="background-image:url('assets/${r.image}');background-size:${r.size};background-position:${r.pos};filter:grayscale(1)"></div><div class="role-card-body"><div class="role-index">角色 ${String(i+1).padStart(2,'0')} · 姓名来自片中对白</div><h3>${r.name}</h3><p>${r.detail}</p><button class="text-btn" data-action="role" data-index="${i}">${icon('volume')}重听这个角色 <kbd>${i+1}</kbd></button></div></article>`).join('')}</div>`;}
function renderRoles(){route='roles';page(shell(`<div class="stage-header"><div class="eyebrow">02 / 认识角色</div><h1>先认识这段视频中的人物</h1><p>故事里有 3 位主要角色。先熟悉他们，听起来就更容易。</p></div>${rolesHTML()}<div class="action-bar"><span class="hint" id="role-hint">${icon('volume')}选择人物，可以单独重听介绍</span><div class="actions"><button class="btn" data-action="roles-all">${icon('replay')}重听全部</button><button class="btn" data-action="chat">修改角色介绍</button><button class="btn primary" data-action="confirm">角色介绍清楚，继续制作 ${icon('arrow')}</button></div></div>`,1),'roles');}
async function playRole(i){if(!roles[i])return;pauseMedia();const done=say('role-'+i,'',true);const row=document.querySelector(`[data-role="${i}"]`);row?.classList.add('speaking');$('#role-hint')&&($('#role-hint').textContent=`正在介绍第 ${i+1} 位，共 3 位`);const success=await done;row?.classList.remove('speaking');return success;}
async function playRoles(){stopGuide();for(let i=0;i<3;i++){if(route!=='roles'&&!modal.open)break;const result=await playRole(i);if(!result)break;}}
const stageIndex=stage=>stage==='roles'?1:stage==='short'?2:stage==='medium'||stage==='verify'?3:4;
function settingsHTML(settings,version){return `<aside class="setting-card"><h3>这次的旁白设置</h3><div class="version">样片版本 ${version} · ${task?.confirmed?.version===version?'已确认设置':'等待你试听确认'}</div><dl class="settings-summary"><div class="setting-item"><dt>旁白语速</dt><dd>${settings.speed==='slow'?'稍慢，更从容':'自然语速'}</dd></div><div class="setting-item"><dt>旁白音量</dt><dd>${settings.gain===1?'更清晰':'标准'}</dd></div><div class="setting-item"><dt>描述重点</dt><dd>${settings.density==='concise'?'关键画面，少说一点':'人物与关键动作'}</dd></div></dl><p class="footnote">设置只用于本次视频。<br>原片对白、音乐和播放速度保持不变。</p><button class="btn small" data-action="chat">${icon('chat')}和 AI 说说问题</button></aside>`;}
function playerHTML(label){return `<div class="player-shell"><div class="video-wrap"><video id="film-player" preload="metadata" playsinline poster="assets/scene-${task?.stage==='verify'?2:1}.png" aria-label="雨停之前，中文模拟短片"><track kind="subtitles" src="assets/dialogue.vtt" srclang="zh-CN" label="中文对白"></video><span class="video-label">${icon('headphones')}<span id="video-label-text">${label}</span></span><div class="play-overlay" id="play-overlay"><button data-action="play" aria-label="播放视频">${icon('play')}</button></div><div class="subtitle" id="subtitle" aria-hidden="true"></div></div><div class="player-controls"><input type="range" id="seek" class="seek" min="0" max="200" value="0" step="0.1" aria-label="视频播放进度"><div class="control-row"><button class="icon-btn" data-action="play" id="play-button" aria-label="播放">${icon('play')}</button><button class="icon-btn" data-action="back10" aria-label="后退 10 秒">${icon('back10')}</button><button class="icon-btn" data-action="forward10" aria-label="前进 10 秒">${icon('forward10')}</button><span class="time" id="player-time">00:00 / 03:20</span><span class="spacer"></span><button class="narration-toggle" data-action="narration" aria-pressed="true">${icon('headphones')}<span>旁白开启</span></button><button class="icon-btn" data-action="fullscreen" aria-label="全屏播放">${icon('fullscreen')}</button></div></div><div class="media-error" id="media-error" hidden></div></div>`;}
function renderSample(){const stage=task.stage;route=stage;const verify=stage==='verify',medium=stage==='medium';const duration=medium?45:7;const heading=verify?'用更新后的设置，再确认 7 秒':medium?'再听 45 秒，看看连续观看是否合适':'先听 7 秒，找到喜欢的旁白';const sub=verify?'换一个镜头，确认刚才的调整在后续内容里也合适。':medium?'沿用你刚才确认的设置，听听连续的故事是否自然。':'听听声音、节奏和描述。想调整的地方，直接告诉 AI。';const primary=medium?(task.mediumEdited?'修改满意，进行短片复验':'满意，制作完整视频'):verify?'复验满意，制作完整视频':'满意，继续制作 45 秒';
 page(shell(`<div class="stage-header"><div class="eyebrow">${medium||verify?'04 / 连续试听':'03 / 试听短片'}</div><h1>${heading}</h1><p>${sub}</p></div><div class="sample-layout"><div>${playerHTML(`${duration} 秒${verify?'复验':'样片'} · 口述版`)}<div class="sample-context">${icon('info')}同一部影片 · ${verify?'01:21—01:28':medium?'00:08—00:53':'00:08—00:15'} · 播放结束后由你决定下一步</div><details class="transcript"><summary>${icon('chevron')}查看这一段的旁白文本</summary><div id="transcript-text"></div></details></div>${settingsHTML(task.candidate,task.version)}</div><div class="action-bar"><span class="hint"><kbd>空格</kbd> 播放 / 暂停 · <kbd>Enter</kbd> 确认当前样片</span><div class="actions"><button class="btn" data-action="replay">${icon('replay')}重听这段</button><button class="btn primary" data-action="confirm" id="confirm-sample" disabled title="等待视频资源准备完成">${primary} ${icon('arrow')}</button></div></div>`,stageIndex(stage)),verify?'verify':medium?(task.mediumEdited?'medium-edited':'medium'):'short');
 const ready=initPlayer(verify?81:8,duration,task.candidate);ready.then(ok=>{if(ok&&route===stage)say(currentGuide);});persist();return ready;
}
async function confirm(){if(!task||['analyzing','generating','full'].includes(route)||(!playerReady&&route!=='roles'))return;stopAll();let next;try{next=confirmStage(task);}catch(e){toast(e.message);return;}persist();if(next==='full'){if(!canComplete(task)){toast('请先确认当前版本的样片。');return;}processing('full','按照你确认的方式，完成整段视频',['为后续镜头补充旁白','检查连续播放与音轨','准备完整成片'],()=>{task.completed=true;task.stage='complete';persist();renderComplete();say('complete');},4);}else{if(next==='medium')toast('本次视频的旁白设置已记住');processing('generating',next==='verify'?'正在准备另一个镜头的 7 秒复验':`正在准备 ${next==='short'?'7':'45'} 秒样片`,['应用本次旁白设置','准备对应样片版本','检查播放资源'],renderSample,stageIndex(next));}}
function renderComplete(){route='complete';page(shell(`<div class="stage-header"><div class="eyebrow">05 / 制作完成</div><h1>你的故事，现在可以听见了</h1><p>按照你确认的旁白设置，整段视频已准备好。</p></div><div class="complete-card"><img src="assets/scene-3.png" alt="雨停后，林夏接过小宇递来的伞"><div><div class="success-line">${icon('check')}完整口述影像已就绪</div><h2>雨停之前</h2><p>3 分 20 秒 · 中文对白与口述旁白<br>${task.candidate.speed==='slow'?'稍慢语速':'自然语速'} · ${task.candidate.density==='concise'?'简洁描述':'适中描述'}</p><button class="btn primary block" data-action="watch-new">${icon('play')}播放完整视频</button><button class="btn block" data-action="save" ${task.saved?'disabled':''}>${icon('save')}${task.saved?'已加入我的视频':'加入我的视频'}</button></div></div>`,4),'complete');}
async function openWatch(item,autoplay=true){stopAll();watching=item;route='watch';page(`<div class="container watch-layout"><div class="back-row"><button class="text-btn" data-action="home">${icon('back')}返回我的视频</button><span class="task-name">中文模拟短片 · 完整版</span></div><div class="watch-heading"><div><h1>${esc(item.title)}</h1><p>一场雨，一间老钟表铺，一段让脚步慢下来的相遇。 · 03:20</p></div><button class="btn" data-action="save" ${library.some(x=>x.id===item.id)?'disabled':''}>${icon('save')}${library.some(x=>x.id===item.id)?'已加入我的视频':'加入我的视频'}</button></div>${playerHTML('完整视频 · 口述版')}<div class="action-bar"><div class="actions"><button class="btn small" data-action="replay">${icon('replay')}从头播放</button><button class="btn small" data-action="watch-roles">查看角色介绍</button></div><button class="text-btn" data-action="caption">${icon('check')}中文字幕开启</button></div><details class="transcript"><summary>${icon('chevron')}查看口述旁白文本</summary><div id="transcript-text"></div></details><p class="inline-note">${icon('info')}原创模拟短片 · 插画镜头运动 · 中文合成对白与旁白</p></div>`,'watch');const ready=initPlayer(0,200,item.settings,item.position<198?item.position:0);const guided=say('watch');await ready;await guided;if(autoplay&&route==='watch'&&watching===item&&!modal.open&&!chat.open)playMedia();}
function saveFilm(){if(!task?.completed)return;if(library.some(x=>x.id===task.id)){toast('这个视频已经在你的视频库中。');return;}const item={id:task.id,title:task.title,description:'雨停之前，按本次确认的设置制作。',settings:{...task.confirmed},position:watching?.id===task.id?watching.position||0:0,created:Date.now()};library.unshift(item);task.saved=true;if(!persist()){library.shift();task.saved=false;return;}document.querySelectorAll('[data-action="save"]').forEach(b=>{b.innerHTML=icon('check')+'已加入我的视频';b.disabled=true;});toast('已加入我的视频，下次可以继续播放');say('saved');}
function freshWatch(){return {id:task.id,title:task.title,settings:{...task.confirmed},position:0};}
function mediaReady(el){return new Promise((resolve,reject)=>{if(el.readyState>=1)return resolve();const timeout=setTimeout(()=>{cleanup();reject(new Error('媒体读取超时，请重试。'));},15000);const cleanup=()=>{clearTimeout(timeout);el.removeEventListener('loadedmetadata',loaded);el.removeEventListener('error',failed);};const loaded=()=>{cleanup();resolve();};const failed=()=>{cleanup();reject(new Error('视频或旁白暂时无法读取，请重试。'));};el.addEventListener('loadedmetadata',loaded,{once:true});el.addEventListener('error',failed,{once:true});});}
async function initPlayer(start,duration,settings,position){
 mediaAbort?.abort();mediaAbort=new AbortController();const signal=mediaAbort.signal;
 const generation=++mediaGeneration;player=$('#film-player');if(!player)return false;const v=player;clipStart=start;clipEnd=start+duration;playing=false;playerReady=false;narrationOn=true;captionOn=true;lastSavedPosition=0;
 v.src=speech.available?'/api/media/rain-before.mp4':'assets/rain-before.mp4';v.playbackRate=1;narration.playbackRate=1;narration.preload='auto';narration.volume=settings.gain;
 $('#seek').max=duration;$('#seek').addEventListener('input',e=>seekTo(clipStart+Number(e.target.value)));
 v.addEventListener('timeupdate',updateProgress);v.addEventListener('ended',()=>{pauseMedia();updateProgress();});v.addEventListener('pause',()=>{if(v!==player)return;narration.pause();playing=false;updatePlayButtons();});
 v.addEventListener('error',()=>{if(v===player)mediaError(new Error('视频读取失败，请检查网络后重试。'));});
 const key=`${settings.speed}-${settings.density}`;activeNarrationCues=film?.narration[key]||[];currentAudioSource='local';
 const label=$('#video-label-text');let src=`assets/${key}.m4a`;
 if(liveSpeech()){
  if(label)label.textContent='正在合成中文旁白…';announce('正在通过豆包语音合成本段旁白。');
  try{const result=await speech.narration(start,duration,settings,signal);if(generation!==mediaGeneration)return false;src=result.url;activeNarrationCues=result.cues;currentAudioSource='volcengine';}
  catch(e){if(signal.aborted||generation!==mediaGeneration)return false;toast(e.message+' 已使用本地演示音频。');}
 }
 if(generation!==mediaGeneration)return false;
 narration.src=src;if(label)label.textContent=`${duration===200?'完整视频':duration+' 秒样片'} · ${currentAudioSource==='volcengine'?'豆包合成旁白':'本地演示旁白'}`;
 const relevant=activeNarrationCues.filter(c=>c.start>=start&&c.start<clipEnd);$('#transcript-text')&&($('#transcript-text').innerHTML=relevant.map(c=>`<p><span class="film-meta">${time(c.start)}</span>${esc(c.text)}</p>`).join(''));
 try{await Promise.all([mediaReady(v),mediaReady(narration)]);if(generation!==mediaGeneration)return false;v.currentTime=Math.max(start,Math.min(clipEnd,position||start));narration.currentTime=v.currentTime;playerReady=true;const confirmButton=$('#confirm-sample');if(confirmButton){confirmButton.disabled=false;confirmButton.removeAttribute('title');}updateProgress();announce((duration===200?'完整旁白':duration+' 秒样片旁白')+'已准备好，'+(currentAudioSource==='volcengine'?'由豆包语音合成。':'使用本地演示音频。'));return true;}catch(e){if(generation===mediaGeneration)mediaError(e);return false;}
}
function mediaError(e){pauseMedia();const box=$('#media-error');if(box){box.hidden=false;box.innerHTML=`${esc(e?.message||'播放未开始，请重试。')} <button class="text-btn" data-action="retry-media">重新加载</button>`;}announce('播放未开始，请重试。');}
function updatePlayButtons(){const btn=$('#play-button');if(btn){btn.innerHTML=icon(playing?'pause':'play');btn.setAttribute('aria-label',playing?'暂停':'播放');}const overlay=$('#play-overlay');if(overlay)overlay.style.display=playing?'none':'grid';}
async function playMedia(){
 if(!playerReady){toast('视频正在准备，请稍候再播放。');return;}
 stopGuide();if(!player)return;const v=player;const generation=mediaGeneration;
 if(v.currentTime>=clipEnd-.08||v.currentTime<clipStart)v.currentTime=clipStart;
 narration.currentTime=v.currentTime;playing=true;$('#media-error').hidden=true;updatePlayButtons();
 try{await v.play();if(narrationOn){narration.currentTime=v.currentTime;await narration.play();}if(generation!==mediaGeneration){v.pause();return;}raf=requestAnimationFrame(syncFrame);}catch{mediaError(new Error('播放未开始，请再次点击播放。'));}
}
function syncFrame(){if(!playing||!player)return;if(player.currentTime>=clipEnd){player.currentTime=clipEnd;pauseMedia();updateProgress();return;}if(narrationOn&&Math.abs(narration.currentTime-player.currentTime)>.20)narration.currentTime=player.currentTime;updateProgress();raf=requestAnimationFrame(syncFrame);}
function updateProgress(){if(!player)return;const at=player.currentTime;const elapsed=Math.max(0,Math.min(clipEnd-clipStart,at-clipStart));$('#seek')&&($('#seek').value=elapsed);$('#player-time')&&($('#player-time').textContent=`${time(elapsed)} / ${time(clipEnd-clipStart)}`);
 const settings=route==='watch'?watching.settings:task?.candidate;const key=settings?`${settings.speed}-${settings.density}`:'normal-balanced';let cue=film?.dialogues.find(x=>at>=x.start&&at<x.end);if(!cue&&narrationOn)cue=(activeNarrationCues||film?.narration[key])?.find(x=>at>=x.start&&at<x.end);if($('#subtitle'))$('#subtitle').innerHTML=captionOn&&cue?`<span>${cue.kind==='narration'?'口述 · ':esc(cue.speaker)+'：'}${esc(cue.text)}</span>`:'';
 if(route==='watch'&&watching){watching.position=at;const entry=library.find(x=>x.id===watching.id);if(entry)entry.position=at;if(Math.abs(at-lastSavedPosition)>4){persist();lastSavedPosition=at;}}
}
function seekTo(at){if(!playerReady)return;const keep=playing;player.currentTime=Math.max(clipStart,Math.min(clipEnd,at));narration.currentTime=player.currentTime;updateProgress();if(keep&&player.currentTime<clipEnd&&!playing)playMedia();}
function replay(){if(!playerReady)return;pauseMedia();seekTo(clipStart);playMedia();}
function toggleNarration(){narrationOn=!narrationOn;const b=$('[data-action="narration"]');b.setAttribute('aria-pressed',String(narrationOn));b.innerHTML=icon('headphones')+`<span>${narrationOn?'旁白开启':'原片声音'}</span>`;$('#video-label-text').textContent=narrationOn?(route==='watch'?`完整视频 · ${currentAudioSource==='volcengine'?'豆包合成旁白':'本地演示旁白'}`:`${clipEnd-clipStart} 秒样片 · ${currentAudioSource==='volcengine'?'豆包合成旁白':'本地演示旁白'}`):'原片 · 口述旁白已关闭';if(!narrationOn)narration.pause();else if(playing){narration.currentTime=player.currentTime;narration.play().catch(mediaError);}updateProgress();announce(narrationOn?'口述旁白已开启':'口述旁白已关闭，正在播放原片声音');}
function showModal(title,content){pauseMedia();stopGuide();returnFocus=document.activeElement;modal.innerHTML=`<div class="dialog-heading"><h2 id="modal-title">${title}</h2><button class="icon-btn" data-action="close-modal" aria-label="关闭弹窗">${icon('close')}</button></div>${content}`;modal.showModal();}
function closeModal(){if(!modal.open)return;stopGuide();modal.close();if(returnFocus?.isConnected)returnFocus.focus();}
function showSettings(){showModal('语音与快捷键',`<p class="dialog-copy">选择最适合你的操作方式。你可以随时停止或重听提示。</p><div class="preference-row"><div><strong>在线语音合成</strong><p>${speech.available?'豆包语音 · 中文女声；关闭后使用本地演示音频':'在线服务未连接，正在使用本地演示音频'}</p></div><button class="switch" role="switch" aria-label="在线语音合成" aria-checked="${liveSpeech()}" data-action="pref-online" ${speech.available?'':'disabled'}></button></div><div class="preference-row"><div><strong>产品语音引导</strong><p>听取当前步骤和操作提示</p></div><button class="switch" role="switch" aria-label="产品语音引导" aria-checked="${prefs.guide&&!prefs.reader}" data-action="pref-guide"></button></div><div class="preference-row"><div><strong>读屏优先</strong><p>关闭站内引导，避免与读屏声音重复</p></div><button class="switch" role="switch" aria-label="读屏优先" aria-checked="${prefs.reader}" data-action="pref-reader"></button></div><div class="preference-row"><div><strong>数字快捷键</strong><p>用 1—9 播放视频或重听角色</p></div><button class="switch" role="switch" aria-label="数字快捷键" aria-checked="${prefs.shortcuts}" data-action="pref-shortcuts"></button></div><div class="dialog-actions"><button class="btn" data-action="stop-guide">停止提示</button><button class="btn primary" data-action="close-modal">完成</button></div>`);}
function showHelp(){showModal('用你习惯的方式操作',`<p class="dialog-copy">所有操作都可以用键盘完成。光标在按钮或输入框里时，优先使用控件本身的按键行为。</p><table class="help-table"><tbody><tr><td><kbd>空格</kbd></td><td>首页创建 · 上传选文件 · 播放或暂停</td></tr><tr><td><kbd>1—9</kbd></td><td>按当前编号播放视频、重听角色</td></tr><tr><td><kbd>Tab</kbd></td><td>移动到下一个操作</td></tr><tr><td><kbd>Enter</kbd></td><td>激活当前按钮，或确认试听</td></tr><tr><td><kbd>Esc</kbd></td><td>关闭弹层，或停止语音提示</td></tr></tbody></table><p class="inline-note">语音输入为预设模拟，可用文字或示例意见完成同样的操作。</p><div class="dialog-actions"><button class="btn" data-action="reset-confirm">重置演示</button><button class="btn primary" data-action="close-modal">知道了</button></div>`);}
function showLink(){showModal('粘贴视频链接',`<form id="link-form"><p class="dialog-copy">当前 Demo 支持下方的预设链接。其他链接可以填写，但尚不支持真实解析。</p><label class="field-label" for="video-url">视频链接</label><input class="text-input" id="video-url" name="url" type="text" inputmode="url" placeholder="粘贴视频链接" autocomplete="off" required><button type="button" class="preset-link" data-action="fill-link">使用预设链接：demo://rain-before</button><div class="dialog-error" id="link-error" role="status"></div><div class="dialog-actions"><button type="button" class="btn" data-action="close-modal">取消</button><button type="submit" class="btn primary">开始解析 ${icon('arrow')}</button></div></form>`);$('#link-form').addEventListener('submit',e=>{e.preventDefault();if($('#video-url').value.trim()==='demo://rain-before'){closeModal();startDemo();}else{$('#link-error').textContent='当前 Demo 未准备该链接的演示结果。请更换链接，或使用上方的预设链接。';announce($('#link-error').textContent);}});$('#video-url').focus();}
async function handleFile(file){if(!file)return;if(!file.type.startsWith('video/')){toast('请选择可播放的视频文件。');return;}stopAll();if(previewUrl)URL.revokeObjectURL(previewUrl);previewUrl=URL.createObjectURL(file);const zone=$('#local-preview');if(!zone)return;zone.innerHTML=`<div class="local-preview"><h3>${esc(file.name)}</h3><video controls playsinline src="${previewUrl}" aria-label="已选择文件预览"></video><p id="file-meta">正在读取视频时长…</p><p>这是你的本地视频预览。当前 Demo 没有准备对应的人物和旁白结果。</p><div class="dialog-actions"><button class="btn" data-action="upload">更换视频</button><button class="btn primary" data-action="demo">使用演示视频体验制作</button></div></div>`;const v=zone.querySelector('video');v.addEventListener('loadedmetadata',()=>$('#file-meta').textContent=`时长 ${time(v.duration)} · 来源：本地文件 · ${(file.size/1024/1024).toFixed(1)} MB`);v.addEventListener('play',()=>stopGuide());v.addEventListener('error',()=>{if($('#file-meta'))$('#file-meta').textContent='当前浏览器无法预览这个视频格式，可以更换文件。';});announce('已选择 '+file.name+'，可以在下方预览。');if(file.size===film?.bytes&&crypto.subtle){const hash=Array.from(new Uint8Array(await crypto.subtle.digest('SHA-256',await file.arrayBuffer()))).map(x=>x.toString(16).padStart(2,'0')).join('');if(hash===film.sha256&&route==='upload'){zone.querySelector('p+p').textContent='已匹配到内置演示素材，预设人物与旁白适用于这段视频。';zone.querySelector('[data-action="demo"]').textContent='继续分析这段视频';}}}
function showExit(){if(route==='upload'||!task||task.completed){goHome();return;}showModal('这次制作，下次接着来',`<p class="dialog-copy">你目前在「${stageNames[task.stage]||'整理素材'}」。保存草稿后返回首页，下次可以从这里继续。</p><div class="dialog-actions"><button class="btn" data-action="close-modal">继续制作</button><button class="btn primary" data-action="save-exit">保存草稿并返回</button></div>`);}
function resume(){closeModal();stopAll();runId++;if(!task)return renderUpload();if(task.completed)return renderComplete();if(task.stage==='roles')renderRoles();else if(['short','medium','verify'].includes(task.stage))renderSample();else if(task.stage==='full'){if(canComplete(task)){processing('full','继续准备完整视频',['加载已确认设置','检查完整视频','准备成片'],()=>{task.completed=true;task.stage='complete';persist();renderComplete();},4);}else{task.stage='medium';renderSample();}}say(currentGuide);}
function openChat(){if(!task)return;pauseMedia();stopGuide();returnFocus=document.activeElement;chat.innerHTML=`<div class="chat-inner"><div class="dialog-heading"><h2 id="chat-title">和 AI 说说问题</h2><button class="icon-btn" data-action="close-chat" aria-label="关闭对话">${icon('close')}</button></div><div class="chat-context">正在修改：${stageNames[task.stage]}<br>当前视频：雨停之前 · 版本 ${task.version}</div><div class="chat-log" id="chat-log"></div><div class="suggestions"><button data-action="feedback" data-text="旁白慢一点">旁白慢一点</button><button data-action="feedback" data-text="旁白大声一点">旁白大声一点</button><button data-action="feedback" data-text="描述少一点">描述少一点</button></div><form class="chat-form" id="chat-form"><label class="sr-only" for="feedback-text">你的修改意见</label><textarea id="feedback-text" placeholder="告诉我，哪里听起来不太合适…"></textarea><div class="dialog-actions"><button type="button" class="btn" data-action="simulate-voice">${icon('mic')}模拟语音输入</button><button type="submit" class="btn primary">发送 ${icon('arrow')}</button></div><p class="chat-note">预设对话演示，不调用麦克风。可以调整旁白语速、音量和描述量。</p></form></div>`;drawChat();chat.showModal();$('#chat-form').addEventListener('submit',e=>{e.preventDefault();const text=$('#feedback-text').value.trim();if(text)feedback(text);});$('#feedback-text').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey&&!e.isComposing){e.preventDefault();$('#chat-form').requestSubmit();}});}
function drawChat(){const log=$('#chat-log');if(!log)return;log.innerHTML=`<div class="chat-bubble"><div class="chat-who">听见 AI · 演示</div>哪里听起来还不够合适？<br>说说你的感受，我们先调整当前这一段。</div>${task.chat.map(m=>`<div class="chat-bubble ${m.who==='user'?'user':''}"><div class="chat-who">${m.who==='user'?'你':'听见 AI · 演示'}</div>${esc(m.text)}</div>`).join('')}`;log.scrollTop=log.scrollHeight;}
function closeChat(){if(!chat.open)return;stopGuide();chat.close();if(returnFocus?.isConnected)returnFocus.focus();}
async function feedback(text){if(!task||!chat.open||chat.dataset.busy==='true')return;task.chat.push({who:'user',text});const result=applyFeedback(task,text);task.chat.push({who:'ai',text:result.message});drawChat();$('#feedback-text').value='';persist();announce(result.message);if(!result.ok)return;chat.dataset.busy='true';chat.querySelectorAll('button:not([data-action="close-chat"]),textarea').forEach(b=>b.disabled=true);const guardTask=task;const guardVersion=task.version;const guardRun=runId;const key=result.changes.some(x=>x.includes('语速'))?'slower':result.changes.some(x=>x.includes('音量'))?'louder':'concise';setTimeout(async()=>{if(task!==guardTask||task.version!==guardVersion||runId!==guardRun){chat.dataset.busy='false';return;}chat.dataset.busy='false';closeChat();const ready=await renderSample();if(!ready||task!==guardTask||runId!==guardRun)return;stopGuide();toast('版本 '+task.version+' 已准备好，请试听后确认');await say(key);},1000);}
function viewStage(n){if(route==='upload'||!task)return;const active=stageIndex(task.stage);if(n>=active)return;if(n===0){showModal('本次视频',`<div class="source-top"><img src="assets/scene-1.png" alt="雨停之前的封面"><div><h3>雨停之前</h3><p>03:20 · 内置演示视频</p></div></div><p class="dialog-copy">当前制作继续使用这段素材。人物介绍、样片和完整影片均来自同一视频。</p><div class="dialog-actions"><button class="btn primary" data-action="close-modal">返回当前步骤</button></div>`);}else if(n===1){showModal('回看角色介绍',rolesHTML()+`<div class="dialog-actions"><button class="btn primary" data-action="close-modal">返回当前步骤</button></div>`);modal.classList.add('wide-modal');}else{showModal('已确认的试听阶段',`<p class="dialog-copy">${n===2?'7 秒样片':'45 秒样片'}已由你确认。当前正在「${stageNames[task.stage]}」。继续试听当前版本，就可以沿着这次制作往下走。</p><div class="dialog-actions"><button class="btn primary" data-action="close-modal">返回当前步骤</button></div>`);}}
function handleAction(action,b){
 switch(action){
 case 'home':case 'library':if(!['home','library','watch','complete','upload'].includes(route)){showExit();}else goHome(action==='library');break;
 case 'create':beginCreate();break;
 case 'new-task':closeModal();task=null;persist();renderUpload();break;
 case 'resume':resume();break;
 case 'upload':$('#file-input').click();break;
 case 'demo':startDemo();break;
 case 'link':showLink();break;
 case 'fill-link':$('#video-url').value='demo://rain-before';$('#video-url').focus();break;
 case 'exit':showExit();break;
 case 'save-exit':if(persist()){closeModal();goHome();}break;
 case 'view-stage':viewStage(Number(b.dataset.stage));break;
 case 'confirm':confirm();break;
 case 'role':playRole(Number(b.dataset.index));break;
 case 'roles-all':playRoles();break;
 case 'watch':{const item=library.find(x=>x.id===b.dataset.id);if(item)openWatch(item);break;}
 case 'watch-new':openWatch(freshWatch());break;
 case 'save':saveFilm();break;
 case 'play':if(playing)pauseMedia();else playMedia();break;
 case 'replay':replay();break;
 case 'back10':seekTo(player.currentTime-10);break;
 case 'forward10':seekTo(player.currentTime+10);break;
 case 'narration':toggleNarration();break;
 case 'caption':captionOn=!captionOn;b.innerHTML=icon(captionOn?'check':'close')+(captionOn?'中文字幕开启':'中文字幕关闭');updateProgress();break;
 case 'fullscreen':$('.video-wrap')?.requestFullscreen?.().catch(()=>toast('当前浏览器不支持全屏显示。'));break;
 case 'retry-media':initPlayer(clipStart,clipEnd-clipStart,route==='watch'?watching.settings:task.candidate,player?.currentTime);break;
 case 'watch-roles':showModal('故事里的三位角色',rolesHTML()+`<div class="dialog-actions"><button class="btn primary" data-action="close-modal">返回观看</button></div>`);modal.classList.add('wide-modal');break;
 case 'chat':openChat();break;
 case 'close-chat':closeChat();break;
 case 'feedback':feedback(b.dataset.text);break;
 case 'simulate-voice':$('#feedback-text').value='旁白慢一点';$('#feedback-text').focus();announce('已模拟填入：旁白慢一点。点击发送，提交这条意见。');break;
 case 'settings':showSettings();break;
 case 'help':showHelp();break;
 case 'close-modal':closeModal();break;
 case 'enable-guide':prefs.guide=true;prefs.reader=false;persist();updateGuideUI();if(route==='home')renderHome();say(currentGuide);break;
 case 'pref-online':{prefs.ttsMode=liveSpeech()?'local':'online';b.setAttribute('aria-checked',String(liveSpeech()));stopAll();persist();updateGuideUI();if(player)initPlayer(clipStart,clipEnd-clipStart,route==='watch'?watching.settings:task.candidate,player.currentTime);break;}
 case 'pref-guide':prefs.guide=!prefs.guide;if(prefs.guide)prefs.reader=false;b.setAttribute('aria-checked',String(prefs.guide));$('[data-action="pref-reader"]').setAttribute('aria-checked',String(prefs.reader));persist();updateGuideUI();if(prefs.guide)say(currentGuide);else stopGuide();break;
 case 'pref-reader':prefs.reader=!prefs.reader;if(prefs.reader){prefs.guide=false;stopGuide();}b.setAttribute('aria-checked',String(prefs.reader));$('[data-action="pref-guide"]').setAttribute('aria-checked',String(prefs.guide));persist();updateGuideUI();break;
 case 'pref-shortcuts':prefs.shortcuts=!prefs.shortcuts;b.setAttribute('aria-checked',String(prefs.shortcuts));persist();break;
 case 'repeat-guide':if(prefs.reader){announce('当前为读屏优先。'+(guideText[currentGuide]||film?.guides[currentGuide]||'可以按 Tab 浏览操作。'));}else say(currentGuide,guideText[currentGuide],true);break;
 case 'stop-guide':stopGuide();break;
 case 'reset-confirm':closeModal();showModal('重置这次演示？',`<p class="dialog-copy">将清除当前设备保存的草稿、观看进度和新作品，恢复内置示例视频。这个操作无法撤销。</p><div class="dialog-actions"><button class="btn" data-action="close-modal">取消</button><button class="btn primary" data-action="reset">确认重置</button></div>`);break;
 case 'reset':stopAll();try{localStorage.removeItem(KEY);location.reload();}catch{toast('无法重置，请检查浏览器存储权限。');}break;
 }
}
document.addEventListener('click',e=>{const b=e.target.closest('[data-action]');if(b&&!b.disabled)handleAction(b.dataset.action,b);});
document.addEventListener('keydown',e=>{
 if(e.isComposing||e.repeat||e.ctrlKey||e.altKey||e.metaKey)return;
 if(e.key==='Escape'){if(chat.open){e.preventDefault();closeChat();}else if(modal.open){e.preventDefault();closeModal();}else stopGuide();return;}
 if(chat.open||modal.open||e.target.closest('input,textarea,select,button,a,video,[contenteditable="true"]'))return;
 if(e.code==='Space'){e.preventDefault();if(route==='home'||route==='library'){if(!prefs.reader){prefs.guide=true;persist();}beginCreate();}else if(route==='upload')$('#file-input').click();else if(route==='roles'){if(guide.src&&!guide.paused)guide.pause();else if(guide.src&&guide.currentTime>0)guide.play().catch(()=>playRoles());else playRoles();}else if(['short','medium','verify','watch'].includes(route)){playing?pauseMedia():playMedia();}}
 else if(e.key==='Enter'&&['roles','short','medium','verify'].includes(route)){e.preventDefault();confirm();}
 else if(prefs.shortcuts&&/^[1-9]$/.test(e.key)){const i=Number(e.key)-1;if(route==='home'||route==='library'){const visible=[...main.querySelectorAll('[data-action="watch"]')];if(visible[i])visible[i].click();}else if(route==='roles'&&roles[i])playRole(i);}
});
$('#file-input').addEventListener('change',e=>{if(e.target.files[0])handleFile(e.target.files[0]);e.target.value='';});
modal.addEventListener('close',()=>{modal.classList.remove('wide-modal');});
modal.addEventListener('click',e=>{if(e.target===modal){const r=modal.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)closeModal();}});
chat.addEventListener('cancel',e=>{e.preventDefault();closeChat();});modal.addEventListener('cancel',e=>{e.preventDefault();closeModal();});
window.addEventListener('pagehide',()=>{persist();stopAll();});
document.addEventListener('visibilitychange',()=>{if(document.hidden){pauseMedia();stopGuide();persist();}});
async function boot(){try{const response=await fetch('assets/film.json');if(!response.ok)throw new Error();film=await response.json();}catch{toast('影片资源尚未准备好，请稍后刷新。');}await speech.discover();renderHome();if(prefs.guide&&!prefs.reader)say('home');registerTools();}
function registerTools(){const context=document.modelContext;if(!context?.registerTool)return;const life=new AbortController();const tools=[{name:'get_audio_description_state',title:'查看口述影像状态',description:'Read the current visible stage, saved videos and candidate narration settings.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true},execute:async input=>{if(Object.keys(input||{}).length)throw new Error('No parameters accepted');return{page:route,stage:task?.stage||null,settings:task?.candidate||null,videos:library.map(x=>({id:x.id,title:x.title,duration:200}))};}},{name:'start_audio_description',title:'开始制作口述影像',description:'Open the upload stage, or offer to resume the existing draft. This does not confirm a sample or save a film.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:false},execute:async input=>{if(Object.keys(input||{}).length)throw new Error('No parameters accepted');beginCreate();return{page:route,draftChoiceOpen:modal.open};}}];for(const tool of tools){try{Promise.resolve(context.registerTool(tool,{signal:life.signal})).catch(()=>{});}catch{}}window.addEventListener('pagehide',()=>life.abort(),{once:true});}
boot();
