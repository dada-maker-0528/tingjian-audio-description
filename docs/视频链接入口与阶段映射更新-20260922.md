# 视频入口与阶段映射

## 已上线

- 保留本地视频上传、拖入本地视频和链接输入；移除独立演示入口。
- 输入框仅提示“粘贴视频链接”。内部接受“哪吒”等片名写法及正常 HTTP(S) 链接，不匹配固定域名、剧集 ID 或分享参数。输入内容存入本次任务，不发起外站下载。
- 链接入口绑定当前《哪吒之魔童闹海》素材；本地上传继续原有真实上传处理服务。
- 保持上传视频→认识角色→首个场景→连续场景→完成整片的组件与操作链路。
- 第 3 步实际播放现有素材 0–10.2 秒，覆盖哪吒和太乙真人出场。另导出独立短片到 work/link-flow-20260922/media/NZ2_S1_intro_10s.mp4。
- 第 4 步仍是同一故事场景的 39.079 秒完整版本。首个短片不是额外场景，不参与成片重复拼接。
- 新增 stage-media.js 映射与现有旁白助手的媒体适配器：修改要求检查、候选试听、放弃、采用、版本历史沿用现有组件。只允许与真实版本效果一致的修改，不能把任意要求都映射为同一 B。
- 原文件不覆盖；采用版本按任务记录，刷新恢复。第 5 步整片映射须匹配已接受 S1，保持场景连续完整覆盖。
- 正常入口和进度标题去除演示用语。技术清单保留素材来源，不虚构实时下载、改写、配音成功。

## 尚未具备的素材

S1 优化 B 与第 5 步扩展成片映射仍为空，不能声称这两个环节已有可播放的新结果。素材未就绪时保留要求与当前视频，继续按钮不误报完成。

当前 S1 仅有电影原声与旁白已混合的单条立体声轨，不能直接替换旁白又保证原声不受损。已检查画面并在 media/S1B-修改草稿.md 记录拟改第三句；等待同段无旁白原片或独立旁白轨，再制作实际 B。

s2a改.rar 内为剪辑工程和 137.93 秒另一个场景的源视频，不是已经导出的 S1/S2 39 秒最终版本；没有重复处理同事正在导出的 S2。

## 后续素材接入

MEDIA_PLANS[影片ID].revised 接 S1B：id、video、duration、effects、cues。effects 必须描述实际听到的变化；如 action_detail=细节，不能同时声称语速或音色也被改变。

extension 接 S1 已采用版本加 S2 的完整文件：baseS1Id、initial{id,video,duration}、scenes（保留 scene-s1，新增 scene-s2，连续范围）、narration；如 S2 有 B，填 revised{id,video,duration,effects,cues}。完整文件中仅含一次 S1 和一次 S2，不含首个 10 秒预览或 S1A+B 两遍。

## 验证与发布

- 本地构建通过；16 项定向测试通过，包括既有助手确认、进度生产者、片名/新链接识别、短片与完整版阶段、任务间版本隔离、错误修改要求与缺少新场景保护。
- 浏览器预览已检查真实媒体加载、文件选择入口、10.2 秒与 39.079 秒播放范围、刷新恢复和手机布局。
- 语音录入组件未改变；未进行真人麦克风到实际 B 视频的验收，因为对应 B 素材未到位。
- 仅发布 12 个相关 public 文件，保留本地其他页面改动，不覆盖线上 backend/server，不重启服务。
- 备份：/opt/tingjian/backups/link-flow-20260922T190006Z
- 线上验证记录：work/link-flow-20260922/online-checks.json。
