# 提示词库对齐旁白助手

2026-09-22 已上线。

按旁白助手现有六类、29 个字段重做提示词库。字段、取值和说明直接读取 assistant/schema.js，完整场景提示词调用既有 compileDraft 生成；未更改助手字段、解析、编译或执行规则。

支持浏览各字段对应提示词、组合多个要求、绑定已有影片人物、只读预览完整场景提示词，以及加入当前助手草稿。加入草稿不执行生成，沿用原场景范围和未修改字段。助手正在处理、尚有未发送文本或草稿版本变化时不覆盖。常用组合仅保存在当前浏览器，人物专属字段不保存为通用组合。旧制作模板、AI 辅助与版本历史保留在次级入口。

另完成已提出的展开编辑/收起需求：加宽助手并增加可阅读高度，底部确认入口保留。

验证：37 项相关测试通过，构建通过；线上 1366×850、1280×650、390×844 浏览器复测通过。覆盖全部 29 字段可达、常用组合保存和加载、原模板返回、人物别名绑定、保留未发送文本、展开收起。库内预览与加入草稿后的助手完整提示词逐字一致，无页面脚本错误，也没有触发生成请求。本轮未重新生成配音、未进行 NVDA 或视障用户验收。

证据：work/prompt-library-20260922/online.json、online-library.png、online-expanded.png、tests.log、build.log、scoped.patch。

线上所有发布文件与本地哈希一致，服务进程和后端文件未变。

回滚备份：/opt/tingjian/backups/prompt-library-20260922T100809Z
