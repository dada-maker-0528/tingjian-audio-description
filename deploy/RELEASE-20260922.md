# 原版设计与实时语音上线记录

用户明确要求“上线吧”后，发布到 [听见](https://tingjian.43.129.168.70.sslip.io/)。

- 版本：`release-20260922T054229Z-upstream-realtime`。
- 发布内容：本分支已验收的原版前端、制作过程动效、真实后端、旁白助手和百炼实时识别，共 103 个源文件；没有发布本机测试作品或录音。
- 运行方式：沿用 `/opt/tingjian` 下独立 Node/systemd 服务及回环端口 5295。Nginx 增加同源 `/api/asr/realtime` 的 WSS 转发。原登录保护、证书、视频理解和配音配置保持。
- 密钥只配置在服务器权限受限的环境文件中，没有放入前端或 Git。

## 实际验收

- 新版在独立目录、独立数据目录启动检查通过，再切换正式入口；健康接口返回本次发布标识。
- 正式 HTTPS 页面和核心脚本的文件摘要与本地批准版本一致，未登录访问仍返回 401。
- 正式域名浏览器经真实百炼返回 30 次中间更新，文字在录音结束前连续回填。首次文字约 4.9 秒，属于本次云端测试值，不沿用本地 1.1 秒的结果。
- O 启动/结束识别，未自动发送；页面没有脚本错误。证据：`work/cloud-release/online-result.json`、`online-during-speech.png`、`video/`。
- 真实断开 WSS 后按 O 重试，完整原句重新识别，原有输入保留、没有重复填字、麦克风仅启动一次：`work/cloud-release/online-reconnect.json`。前两次短输入检查未匹配固定词“你好”，未计为通过；后一次给测试音频增加静音前导，并在断线前确认完整原句已识别，再核验重试结果。没有给产品增加等待。
- 原有 13 个作品、643 个数据文件摘要均保持，未删除或覆盖。原列表中的两项内置影片助手记录因 `assistantCatalog` 标记被新版过滤，不是用户作品丢失。
- 三个现有用户作品的视频及旁白共六项 Range 读取均返回 206，浏览器视频实际播放通过；Tab/Shift+Tab 导航通过。证据：`work/cloud-release/media-keyboard.json`。
- 同机其他项目的服务进程和启动时间保持不变。临时检查端口 5299 已关闭。

## 回退

备份目录：`/opt/tingjian/backups/release-20260922T054229Z-upstream-realtime`，保留旧版本指向、原环境文件、原 Nginx 配置、服务配置及完整数据备份。

旧代码：`/opt/tingjian/releases/release-20260922T015645Z-assistant`。必要时恢复备份环境和代理，将 `/opt/tingjian/current` 指回旧代码，检查 Nginx 后重启听见服务并重新验收。数据未做迁移，常规代码回退不应覆盖上线后的新增作品。

## 验证边界

语音验收以真实中文录音作为浏览器麦克风测试输入，实际请求百炼。未验证用户个人麦克风、噪声环境、真人读屏或多人并发。本次未重复执行上传到整片生成的全部付费模型流程，沿用当前代码已通过的本地检查并核验线上配置、原有媒体及实时输入。

## 后续小改：删除助手标题右侧三个按钮

按用户截图要求删除“停止播报、返回视频、使用帮助”三个标题栏按钮，仅更新 `public/assistant/view.js` 与 `public/styles.css`，保留原标题区域高度。正式站同视口检查：三个按钮均不存在，场景选择、模式标签和输入框边界位置不变；高级编辑六分类、普通模式输入与语音入口正常，无页面脚本错误。没有重启服务。

此次备份：`/opt/tingjian/backups/remove-assistant-tools-20260922T060123Z`。改前、改后截图及检查记录：`work/remove-assistant-tools/`。本次两个文件的最新摘要见该目录 `after.json`，替代初次发布时相应文件的摘要。


## 演示链路修复发布

版本 `release-20260922T064412Z-demo-hotfix`。修复首段修改破坏完整旁白、角色结构错误重试、短句超长阻断、误标模拟、保存入口和小屏布局。随后热更新播放器：优先读取已生成音轨，准备完成前禁用播放，主动暂停不误报失败。实际测试与边界见 [比赛联测说明](../docs/比赛联测说明-20260922.md)。

发布前核对线上源文件与本地基准一致，独立目录预启动通过，再检查无进行中的任务并切换。所有作品状态文件备份，媒体与旧发布目录保留；环境只更新发布标识，代理及模型配置保持。备份：`/opt/tingjian/backups/release-20260922T064412Z-demo-hotfix`。两次前端热更新各保留修改前文件，不中断生成任务。回退代码时不要覆盖上线后新增作品。

本机验证记录及最终文件摘要：`work/hotfix-20260922/`。旧缺失旁白修复为新版本写入，保留历史；确认修改以外的后续音频逐采样与原版一致。


## 智享视界开屏介绍

按用户明确要求及飞书《修改建议》第一点，将重复片库首页改为项目简介。项目名“智享视界”，队名“产品大王队”；沿用用户给定黑白光影原图。四字标题采用与原 Logo 接近的圆润黑体，以 GenSen Rounded 2 Bold 字形排版并导出矢量轮廓，字体来源和 OFL 许可随素材保留，无运行时字体外链。

原听见三份 Logo 文件逐一核对摘要不变。开屏支持点击、Enter、空格进入视频库，提供主动朗读简介，尊重减少动态效果设置。首页引导已改为介绍，不再播报旧片库数量；已有制作流程沿用。

仅热更新前端 9 个文件，未重启服务、未修改作品数据。备份：`/opt/tingjian/backups/opening-20260922T070730Z`。90 项测试及构建通过；1440×900、1366×768、390×844 验证开屏到视频库、创建入口、返回及键盘进入。证据和源文件摘要：`work/opening-20260922/`。本次未扩展修改文档中其他建议，未声称完成真人读屏验收。
`圆体标题与焦点样式备份：/opt/tingjian/backups/rounded-title-20260922T071211Z`。最终源文件摘要见 `work/opening-20260922/publish-final.json`。
