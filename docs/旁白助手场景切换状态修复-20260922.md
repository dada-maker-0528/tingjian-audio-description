# 旁白助手场景切换状态修复

## 问题与处理

采用新配音后，场景草稿仍停留在 `result` 状态，且保留修改计数；进入下一阶段重新打开草稿时复用了旧结果，播放位置同步又会避开此类草稿，导致旧结果卡一直显示。

- “满意并继续 / 完成”后，将已采用的结果归入当前基准，收起旧结果卡，恢复播放器焦点和场景跟随。
- 重新打开场景时，自动修复此前保存的“结果已全部采用，但结果卡仍未结束”状态。
- 已采用配音、设置和历史对话保留；只改当前场景不会影响其他场景，已确认的后续继承继续有效。
- 未采用的结果、多场景结果中尚未完成的确认、未发送的输入及修改草稿继续保留。

修改限于 `public/assistant/model.js` 与 `public/assistant/view.js`。没有更改声音参数、旁白生成流程或后端接口。

## 验证记录

- 新增 5 项回归用例：旧代码 4 项失败、1 项通过；修复后全部通过。
- 助手模型、确认流程与场景跟随共 26 项测试通过。
- 后端助手及场景流程共 10 项测试通过。
- 构建通过。
- 浏览器使用本地修复文件连接真实服务：生成 +3 dB 配音、采用后进入连续场景、播放切到场景 2、返回场景 1、保留未发送输入、刷新修复旧保存状态，共 5 组检查通过，无页面脚本异常。
- 已部署到演示站，替换前后逐文件核对 SHA-256。备份：`/opt/tingjian/backups/scene-follow-20260922T105909Z`；两个服务进程未重启，后端文件未修改。
- 线上文件无替换拦截的完整复测：上述 5 组浏览器检查全部通过，真实生成及采用成功，无页面脚本异常。
- 浏览器及上线验收详见 `work/scene-follow-20260922/local.json`、`online.json` 和 `deployment.json`；只有相应报告标记通过才表示该阶段完成。

自动化验证不等同于盲人用户的实际读屏验收。
