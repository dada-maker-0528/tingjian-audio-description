# P2 定向修复与上线验证

入口：https://tingjian.43.129.168.70.sslip.io/

按用户本轮要求处理，未修改链接解析入口及既有结果横幅文案，未增加界面上的模式说明。

## 改动

1. 原先因没有对应视频版本而被阻止的旁白修改，可以确认执行、查看结果和采用；沿用当前视频文件。记录保留 `mediaUnchanged=true`、`audioGenerated=false`，不会覆盖此前已采用的视频。原先已匹配到具体视频的路径保留。
2. 场景标题缺乏本段原声关系证据时使用中性称呼。已存在上传项目和助手上下文也统一处理，因此本次测试项目中的“父子骑电动车”现在显示为“两人骑电动车”。同时补充人物识别提示，要求同一人物跨场景保持一致，避免仅因头盔或场景变化拆成不同角色；这不等于所有 AI 身份识别已人工核验。
3. 百炼实时识别失败时继续保留完整录音，结束后自动调用 MiniMax 整段识别，结果替换实时临时文字，避免重复拼接。两条通道均失败时仍保留录音，可重试而无需重说。设备临时启动或约束错误有一次简化参数重试；明确拒绝权限不会反复弹出权限请求。英文权限错误改为中文恢复提示，保留文字输入。
4. 修改确认区去除重复说明，详细内容可展开；只滚动方案内容，确认操作区保持可见。覆盖普通输入、确认卡及恢复草稿场景；窄屏进入修改方案时将助手滚动到可操作位置。

## 验证

- `npm test`：133 项通过。
- `npm run build`：通过。
- 浏览器覆盖 1366×768 和 390×844；桌面确认按钮约 y=633，高 44 像素；窄屏预发布确认及补充状态均在 y=772 附近，高约 44 像素。线上最终记录见证据目录。
- 线上原先受限的哪吒修改及双场景作品第二场景修改均能执行、采用，执行前后播放器视频地址一致。
- 实际百炼实时识别：录音过程中出现 8 次中间结果，停止后出现最终文字。
- 主动模拟实时连接中断，保留实际语音文件经浏览器麦克风测试输入采集的录音，备用接口真实返回 HTTP 200、`provider=minimax` 和中文识别文本。发布后直接访问线上再次验证通过。
- 权限拒绝直接显示中文引导，没有丢失已有输入。
- 线上项目详情和旁白助手上下文的场景名称均返回“两人骑电动车”。
- 发布前后核对文件摘要，保留线上与本地不同的已有保护逻辑；没有覆盖其他未发布修改，也没有修改任何视频文件。

测试语音使用浏览器的音频文件输入；不等同于用户本人麦克风硬件、系统权限或噪声环境验收。浏览器拒绝麦克风权限时无法靠识别模型切换取得声音，需要用户允许网站访问麦克风。

## 发布与证据

- 10 个文件的主修复备份：`/opt/tingjian/backups/p2-repairs-20260922T205356Z`。
- 2 个布局文件的追加修复备份：`/opt/tingjian/backups/p2-layout-20260922T205658Z`；追加修复未重启服务。
- 服务目录仍为 `/opt/tingjian/releases/release-20260922T162337Z-s1-demo`。
- 本地证据：`E:/gpt管理/tingjian-upstream-design/work/p2-fixes-20260922/`，包括测试日志、浏览器截图、真实语音响应、发布清单、文件摘要和线上验证摘要。
