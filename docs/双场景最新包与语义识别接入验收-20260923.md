# 双场景最新包与语义识别接入验收

以用户指定的《双场景最新接入包-20260923.zip》为准。旧文件保留历史，不再用于当前 S2 播放。

## 当前链路

- 首个短片：S1A 的 0–10.2 秒，保留原流程。
- 完整初版：S1A，39.079 秒。
- 第一场景修改版：S1B，39.079 秒，版本 s1-b-208339f0。
- 第二场景：修正版 S2B，137.927007 秒，assets/NZ2_S2B_9e05b035.mp4。
- 完整成片：S1B＋修正版 S2B，176.983333 秒，assets/NZ2_full_86f34f49.mp4。实际交界时间 39.066667 秒。首个 10 秒预览不重复拼入。
- 完成页和完整播放页均提供“下载完整视频”；加入视频库后重新打开仍播放完整成片。
- 所有新视频直接使用已混好的原声和旁白；独立 narration 元素 src 为空并暂停。

## 提示词库与语义识别

提示词库、六类 29 字段、高级编辑、完整提示词和原旁白助手设计保留。本轮未改 CSS。
原固定视频入口绕过了模型语义层，且严格字段匹配会让旧草稿或不同表达卡住；此前其他基础输入也主要依赖本地规则，不能声称当时已普遍调用模型。
新增 /api/assistant/interpret，复用现有模型配置，并读取当前制作提示词库以及同一套字段目录。记录 provider、model、promptRevision、promptHash、catalogHash。识别结果先通过字段和人物校验，再形成待确认方案。
“老是说他，我听不出来是谁在做什么”已在线识别为人物指代；“别啰嗦了，挑重点说就行”识别为精简；新增动作过程与方向细节也按原意识别。单次实测约 2 秒，不是延迟承诺。
实际可试听 S1B 的变化为人物指代明确和措辞精简。若语义结果包含更多要求，确认页会明确实际可试听的两项以及保持原样的内容，原要求留在对话和识别记录中。只生成候选，不自动执行或采用。纯动作细节、语速等不匹配请求可通过“查看可试听的修改方案”恢复，不伪称这些修改已做。
修复模型返回空修改/解释与字段结构不一致时的处理；保留当前方案，不因此把已确认草稿锁死。语义服务失败时才退回本地规则，状态标为 local-fallback。

## 已验证

1. 包内 manifest 所列 10 个文件逐项校验大小与 SHA256；新 S2B、完整成片完整解码无错误。
2. 最终构建通过；29 项定向测试通过，覆盖提示词字段、语义输出校验、旧草稿恢复、确认/执行/采用隔离、版本映射与最终成片条件。
3. 预览与线上浏览器实际走通入口、首个短片、S1A、S1B 回看/放弃/采用、修正版 S2B、85 秒定位播放、完整成片交界播放与片尾暂停、保存后重新打开、手机布局。
4. 线上点击下载并保存 60,572,292 字节 MP4，SHA256 为 86f34f496305e456ad5563f99a805c3ce2cad2d694455e319283a31cba54dd0a，与用户包中最终文件相同。
5. S1B、修正版 S2B、完整成片的 HTTP Range 均返回 206；媒体无额外旁白叠加；浏览器未记录页面脚本错误。

详细记录：work/latest-scenes-20260923/online-checks.json、semantic-online-checks.json、decode-checks.json。预览语义使用测试桩；上述线上复测实际调用模型，记录保留 provider=model，二者不混作同一证据。
本轮没有真人麦克风端到端复测，也未宣称专业视障用户全片听审。素材内容和交界听感确认来自交付包，技术播放、切换与下载由本轮验证。

## 发布与回退

在当前活动发布目录上做限定文件更新，保留无关本地和服务器改动。新增语义接口需要重启 Node 服务，发布后健康检查通过。
主备份：/opt/tingjian/backups/latest-scenes-semantic-20260922T200644Z。
后续修复备份依次：20260922T201228Z、20260922T201355Z、20260922T201553Z（同一 latest-scenes-semantic- 前缀）。
未改凭据、制作提示词库数据或历史项目。视频采用带内容校验值的新文件名，避免旧缓存。

更新后的操作手册：E:/gpt管理/影石/交付/听见-演示讲稿与操作手册.html。
