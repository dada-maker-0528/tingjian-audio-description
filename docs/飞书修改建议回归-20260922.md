# 飞书修改建议回归

2026-09-22，已按[修改建议](https://xcn0lzdwnaky.feishu.cn/wiki/C8Q7wIAjiibkZ1k86ngc4HrTnLh)及用户补充要求更新[听见测试站](https://tingjian.43.129.168.70.sslip.io/)。

目标源码为 `E:\gpt管理\tingjian-upstream-design`，分支 `codex/upstream-design-code-flow`。修改前已逐文件核对当前线上摘要，不能使用旧 `影石` 或 `tingjian-audio-description` 工作副本覆盖当前站点。

| 建议 | 最终状态 |
| --- | --- |
| 首页与我的视频重复 | 当前版本已将首页改为项目介绍；已验证只有“我的视频”展示片库，保留现有项目介绍。 |
| 视频数量与播报不一致 | 标题数量、搜索结果和语音都按当前展示卡片计数，搜索无结果时为 0。 |
| 解析完成提示 | 展示并播报“解析已完成，按空格继续”，实际按空格可进入人物介绍。 |
| 删除修改角色介绍 | 当前版本已移除，浏览器确认没有该按钮。 |
| 样片完成提示 | 展示并播报“样片已就绪，按空格继续下一步”，实际按空格可进入试听。 |
| 助手布局顺序 | 修改摘要及“听修改指令” → 修改设置与应用范围 → 执行与保留内容 → 完整提示词 → 补充要求和确认执行。设置可编辑且与提示词同步。 |
| 失败取消和错误去重 | 只在助手中显示失败原因，提供“取消调整”；取消清除失败草稿，保留既有配音与已采用版本。 |
| 删除配音设置 | 移除播放器下方该面板；保留顶部无障碍语音设置。 |
| 撤下截图中的测试视频 | 按具体项目 ID 隐藏 5 个条目：两部 QA 测试片、提示词联动测试素材、release-short、韩阳第二集上传片；服务端素材和历史保留，未来新上传不受影响。 |

## 实际验证

- 92 项自动测试全部通过，Node 构建通过。
- 本地待发布文件及正式 HTTPS 页面均执行浏览器回归：数量与搜索、空格继续、人物页、助手编辑与提示词同步、模拟失败和取消后播放。
- 390、768、1440 像素视口无横向溢出，最终桌面和手机截图已查看。
- 线上只显示《创业之路》1 部。真实语音请求为“我的视频，共 1 部。”及“当前是我的全部视频，共 1 个条目……”；浏览器实际开始播放入口提示，时长约 2.78 秒。
- 两条完成提示均调用真实语音服务并在浏览器解码：解析完成约 2.78 秒，样片就绪约 3.57 秒。语音缓存按完整文本区分，不会复用旧数量音频。
- 5 个前端文件的 HTTPS 内容摘要与本地一致。后端 24 个文件摘要、听见与同机其他已核对服务进程状态保持不变，无服务重启。
- 失败恢复使用拦截的测试错误，未向服务端实际执行该修改；未做真人审听、NVDA 或新一轮完整 AI 生成验收。

## 证据与回退

本地证据位于 `work/feishu-fixes-20260922/`：`online-verification.json`、`audio-verification.json`、`deployment.json`、`tests.log`、`build.log` 及 `online-*.png`。

服务器版本仍为 `release-20260922T064412Z-demo-hotfix`，本次仅更新 `public/experience.js`、`public/task-progress.js`、`public/assistant/view.js`、`public/styles.css`、`public/library.js`。更新前原件保存在 `/opt/tingjian/backups/feishu-fixes-20260922T074601Z`。
