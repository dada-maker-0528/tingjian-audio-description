# 原版设计与真实后端合并记录

基底：`dada-maker-0528/tingjian-audio-description`，`origin/main` 的 `5baecadc49b9db60c2b608ea4d07bd3c260525a1`。
新分支：`codex/upstream-design-code-flow`。本地预览：http://127.0.0.1:5298/（5297 自动转到此地址）。用户随后明确授权上线，现已发布 `release-20260922T054229Z-upstream-realtime`，见 [上线记录](deploy/RELEASE-20260922.md)。下方“未部署”描述属于此前本地实施阶段。

本轮按用户最后确认，迁入的是**整套制作过程页设计**，包括返回栏、五阶段导航、标题和图标区、进度条、阶段展开与收起、代码流动与回看、暂停/继续、完成状态与下一步按钮。覆盖内容解析、首场景准备、连续场景准备、整片准备。

进度条使用 2 秒线性过渡。各阶段完整播放，不因资源提前返回而跳过代码或后续阶段；最后进度条到达 100% 后才显示完成标题及下一步按钮。暂停冻结代码展示与进度条，继续后接着展示。系统开启减少动态效果时直接显示完整内容。底层请求无需等待动画。

首页、视频库、人物页、播放工作台、普通模式与高级编辑继续使用原仓库设计。保留原版全局样式、助手布局、29 个字段和完整场景流程；本轮按用户批准接入真实 Node 后端。

## 本轮改动文件

| 文件 | 改动 |
|---|---|
| `public/experience.js` | 把原有制作入口接入过程页；按完整场景计算范围；离开页面取消展示及检查 |
| `public/index.html` | 加载过程页样式 |
| `public/task-progress.js` | 移入原过程展示组件，完整播放、回看、暂停、失败和取消处理 |
| `public/task-progress.css` | 原过程页视觉与代码动效；过程页布局规则限定作用域 |
| `public/demo-progress.js` | 读取预设人物和场景、准备已有配音、实际检查视频和音轨资源 |
| `public/task-code.js` | 展示本分支真实文件的实现片段，去除本分支不存在的后台代码引用 |
| `tests/task-progress.test.mjs` | 完整队列展示顺序和失败不能进入成功状态的检查 |

## 用户已批准的 1–6 项

以下六项已获用户批准并完成代码接入。真实模型测试和参数检查的范围分别列在下方。

| 编号 | 原仓库 / 当前新分支 | 我们原有工程的额外能力 | 本轮状态 |
|---|---|---|---|
| A | 原版首页、视频库、人物介绍、播放工作台及旁白助手设计 | 上一版改动过这些页面布局 | 保留原仓库设计 |
| B | 首个完整场景 → 连续完整场景 → 全片 | 旧流程含按秒截取的样片阶段 | 保留原仓库的完整场景流程 |
| C | 原版制作等待页 | 整套过程页、分阶段代码流、展开回看、暂停和下一步 | **已迁入** |
| 1 | 旁白助手主要生成文案与参数预览，不据此重新配音 | 真实旁白改写、配音与时长适配的后端接口 | 已接入原版 UI；实际改写、配音、试听、采用；29 字段的全部选项参与执行计划 |
| 2 | 用户上传文件用于本地预览；示例影片体验制作流程 | 上传视频后的真实处理与自动收听流程 | 已接原版上传与完整场景流程；复用首次分析的场景和原声空隙 |
| 3 | 原仓库品牌图标与文字 | 按用户参考图绘制的 SVG 标志和“听见”字标 | 已替换 SVG 标志、字标与页签图标 |
| 4 | 原仓库语音交互实现 | 服务端语音识别、助手朗读接口及补充的焦点处理 | 已接百炼实时 ASR 边说边填字、O 快捷键、录音重试、焦点朗读与弹窗焦点管理 |
| 5 | 未提供独立提示词库管理入口 | 提示词库、修订及历史记录 | 已加入提示词库及其编辑、修订、历史界面 |
| 6 | 原版模拟方案及浏览器本地版本状态 | 真实配音版本确认、后端任务和产物管理 | 已接真实采用、原版保留、音轨历史回看及后续场景沿用 |

六项均按用户本轮确认实施，无需再次选择。未改旧工程，未部署线上。

## 本轮真实验证与限制

- 语音服务：助手现已改为百炼 `qwen3-asr-flash-realtime`，真实中间结果持续回填；不再等待整段识别，不依赖浏览器 SpeechRecognition。此前 MiniMax 整段识别的测试仅作为历史记录。
- 浏览器中按 O：空输入框可启动识别；说话过程中持续填字；再按 O 结束，回车才发送。输入框已有文字时保留正常输入 O；有待重试录音时 O 可直接重试。中文输入法组合输入不触发快捷键。
- 手动点击语音 → 故意让首次 ASR 请求返回失败 → 按 O 用保留录音调用真实 ASR → 成功填字；麦克风只启动一次，原有文字保留，未自动发送。
- 实际云端改写“动作不用讲得太细”、真实 TTS 音轨、音量调整、试听、采用及历史界面均在原版助手完成。
- 29 个字段及全部枚举选项经过执行计划检查；音量隔离、文案改写、音色/停顿、画面文字及后续场景沿用有集成检查。这不代表逐个选项都完成了真人主观听感验收。
- Tab 从首页移至我的视频、Shift+Tab 返回；提示词弹窗焦点循环；高级编辑六分类；390 × 844 页面无横向溢出，浏览器无脚本错误。
- 浏览器使用 Chromium headless shell 1200；桌面 1440 × 1000。语音测试把真实录制的中文音频作为麦克风输入，调用真实 ASR；尚未验证用户自己的麦克风设备、噪声环境或 NVDA 独立使用。
- 真实上传测试：12.2 秒测试素材（明确移除原声以隔离配音链路）已完成分析、首场景、连续场景、整片，生成 5 条旁白；模型格式及重复分段问题修复后通过重试完成，未把先前失败记录当成成功。已从后端视频库重新打开并验证实际音视频播放同步。证据：`work/real-check/upload-result.json`、`upload-playback.json`。
- 自动检查：本次 89 项前端/接口集成检查通过；此前 20 项后端分析/脚本检查通过。本次 Node 构建通过。
- 原片持续对白且无可用空隙时，必需旁白仍可能被时长/原声保护拦下；会保留原片和已有结果并报错，未标记成功。可省略旁白在确实无空隙时保留原声，记录未插入原因。

## 本轮改动范围与运行

- `public/assistant/`：保持原版普通模式、高级编辑和输入框；新增服务端语音适配与真实任务适配。声音失败保留录音，重试不要求重说。
- `public/experience.js`、`public/upload-service.js`：真实上传、完整场景播放、候选试听、采用、视频库与历史音轨；制作页动效仍完整展示，进度条保持原有 2 秒过渡。
- `backend/`、`server/local.mjs`：原有处理、ASR、配音、提示词和版本服务；复用首次真实分析，避免片段再次分镜；后续制作保留已确认的真实分段音轨，避免重复改写；继承选项作用到真实后续音轨。
- `public/index.html`、SVG 品牌资源、提示词库及弹窗焦点组件：批准的 3–5 项。
- `scripts/build-server.mjs`：默认构建产物改为包含原版前端和真实后端的 `dist/node`；`build:worker` 仅保留上游旧 Worker 构建，不包含这次真实制作后端，不能拿它发布本版本。
- 本地服务端口 5298，运行 `server/local.mjs`。本地加载旧工程原有模型环境文件和本分支忽略的 `.env` 实时 ASR 配置，数据隔离在本分支 `.aimedia`，不读取或覆盖旧作品。凭据、录音和项目数据不入 Git。
- 独立运行需 Node 22、FFmpeg/FFprobe、`ws` 依赖及原工程所需模型环境配置；`npm run build` 只构建，不部署。

## 可复查证据

| 证据 | 文件 |
|---|---|
| 修改前界面 | `work/design-baseline/` |
| 真实按 O 识别与完整改写记录 | `work/real-check/results.json` |
| 手动启动、失败后复用录音实测 | `work/real-check/asr-retry.json`、`asr-retry.png` |
| 真实新配音、普通模式 | `work/real-check/real-result.png`、`real-rewrite.png` |
| 原版高级编辑、窄屏 | `work/real-check/advanced.png`、`mobile.png` |
| 导航与弹窗键盘检查 | `work/real-check/keyboard-layout.json` |
| 真实交互录像 | `work/real-check/video/` |

## 实时语音与上云准备

- `public/assistant/live-recognition.js`：同源 WS/WSS 上传，真实中间结果替换回填，完成结果去重；中断保留录音重试。
- `server/realtime-asr.mjs`、`server/local.mjs`：服务端连接百炼、保护密钥、检查网站来源、会话时限与连接心跳。
- `.env.example`、`deploy/REALTIME-ASR.md`、`deploy/nginx-realtime.conf.example`：云环境变量和 HTTPS 长连接代理配置。
- 实际中文测试收到 27 次中间结果，首字约 1.1 秒；断开连接后按 O 成功复用录音，麦克风只启动一次。证据：`work/real-check/realtime-browser.json`、`realtime-reconnect.json`。
- 本地 HTTPS/WSS 代理已接真实百炼验证停止前填字：`work/real-check/realtime-https.json`。测试脚本通过断言后未自然退出，已结束该测试进程；本地 5298 主服务保持运行。尚未部署或验证真实云代理及用户麦克风，不能承诺网络永不掉线。
