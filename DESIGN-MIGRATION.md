# 本地设计迁移与待选合并项

基底：`dada-maker-0528/tingjian-audio-description`，`origin/main` 的 `5baecadc49b9db60c2b608ea4d07bd3c260525a1`。
新分支：`codex/upstream-design-code-flow`。本地预览：http://127.0.0.1:5297/ 。本轮没有部署线上。

本轮按用户最后确认，迁入的是**整套制作过程页设计**，包括返回栏、五阶段导航、标题和图标区、进度条、阶段展开与收起、代码流动与回看、暂停/继续、完成状态与下一步按钮。覆盖内容解析、首场景准备、连续场景准备、整片准备。

进度条使用 2 秒线性过渡。各阶段完整播放，不因资源提前返回而跳过代码或后续阶段；最后进度条到达 100% 后才显示完成标题及下一步按钮。暂停冻结代码展示与进度条，继续后接着展示。系统开启减少动态效果时直接显示完整内容。底层请求无需等待动画。

首页、视频库、人物页、播放工作台、普通模式与高级编辑继续使用原仓库设计。没有替换其全局样式、助手组件、29 个字段、完整场景划分或服务实现。

## 本轮改动文件

| 文件 | 改动 |
|---|---|
| `public/experience.js` | 把原有制作入口接入过程页；按完整场景计算范围；离开页面取消展示及检查 |
| `public/index.html` | 加载过程页样式 |
| `public/task-progress.js` | 移入原过程展示组件，完整播放、回看、暂停、失败和取消处理 |
| `public/task-progress.css` | 原过程页视觉与代码动效；过程页布局规则限定作用域 |
| `public/demo-progress.js` | 读取预设人物和场景、准备已有配音、实际检查视频和音轨资源 |
| `public/task-code.js` | 展示本分支真实文件的实现片段，去除本分支不存在的后台代码引用 |
| `tests/task-progress.test.mjs` | 完整队列展示顺序和失败不能进入成功状态的检查 |

## 两边主要差异与待选择项

以下按代码差异整理，不代表这些旧版功能已经在新分支接通或通过验证。除过程页以外均未迁入。

| 编号 | 原仓库 / 当前新分支 | 我们原有工程的额外能力 | 本轮状态 |
|---|---|---|---|
| A | 原版首页、视频库、人物介绍、播放工作台及旁白助手设计 | 上一版改动过这些页面布局 | 保留原仓库设计 |
| B | 首个完整场景 → 连续完整场景 → 全片 | 旧流程含按秒截取的样片阶段 | 保留原仓库的完整场景流程 |
| C | 原版制作等待页 | 整套过程页、分阶段代码流、展开回看、暂停和下一步 | **已迁入** |
| 1 | 旁白助手主要生成文案与参数预览，不据此重新配音 | 真实旁白改写、配音与时长适配的后端接口 | 待选：接入原版助手 UI，并逐项验收全部选项 |
| 2 | 用户上传文件用于本地预览；示例影片体验制作流程 | 上传视频后的真实处理与自动收听流程 | 待选：适配原版上传入口 |
| 3 | 原仓库品牌图标与文字 | 按用户参考图绘制的 SVG 标志和“听见”字标 | 待选：只换品牌资源 |
| 4 | 原仓库语音交互实现 | 服务端语音识别、助手朗读接口及补充的焦点处理 | 待选：核对原版已有能力后选择性迁移 |
| 5 | 未提供独立提示词库管理入口 | 提示词库、修订及历史记录 | 待选：会增加管理入口 |
| 6 | 原版模拟方案及浏览器本地版本状态 | 真实配音版本确认、后端任务和产物管理 | 待选：建议与第 1 项一起适配 |

建议优先评估 1、2、6，保留原版界面，仅接真实能力。品牌资源 3 可独立选择；4、5 按使用需要决定。

## 验证与限制

- 使用 Chromium headless shell 1200，桌面视口 1440 × 1000，窄屏 390 × 844。
- 修改前截图：`work/design-baseline/`；修改后截图、检查结果、真实浏览器录像：`work/design-after/`。
- 首页 / 助手对照采用相同视口；播放器和助手关键元素边界位置与尺寸一致。
- 浏览器走通预设素材的内容解析、首场景、连续场景、整片；检查代码回看、暂停/继续、键盘下一步、Shift+Tab、窄屏与减少动态效果。
- 本地未配置在线语音凭据，本轮媒体检查使用真实可播放的预设视频与本地音轨。没有据此声称验证真实旁白重制、所有选项效果或真实用户盲用验收。
- 未更改旧工程的已有修改，未覆盖线上。
